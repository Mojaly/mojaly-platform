{
  "query": "query GetAssetsQuery($first: Int) { assets(first: $first) { edges { node { id code scale withdrawalThreshold createdAt } } pageInfo { hasNextPage endCursor } } }",
  "variables": {
    "first": 100
  }
}