services:
  redis:
    image: ${REDIS_IMAGE}
    container_name: mojaly-rafiki-redis
    restart: unless-stopped
    networks:
      - rafiki
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 5s
