version: v0.13.0

dsn: postgres://postgres:password@postgres:5432/mojaly_kratos?sslmode=disable&max_conns=20&max_idle_conns=4

serve:
  public:
    base_url: https://admin.mojaly.local/kratos/
    cors:
      enabled: true
  admin:
    base_url: http://kratos:4434/

selfservice:
  default_browser_return_url: https://admin.mojaly.local/
  allowed_return_urls:
    - https://admin.mojaly.local

  methods:
    link:
      config:
        lifespan: 1h
        base_url: https://admin.mojaly.local/kratos
      enabled: true
    password:
      enabled: true

  flows:
    error:
      ui_url: https://admin.mojaly.local/error

    settings:
      ui_url: https://admin.mojaly.local/settings
      privileged_session_max_age: 15m
      required_aal: highest_available

    recovery:
      enabled: true
      ui_url: https://admin.mojaly.local/auth/recovery
      use: link
      after:
        hooks:
          - hook: revoke_active_sessions

    verification:
      enabled: false

    logout:
      after:
        default_browser_return_url: https://admin.mojaly.local/auth

    login:
      ui_url: https://admin.mojaly.local/auth/login
      lifespan: 10m

    registration:
      enabled: false

log:
  level: debug
  format: json
  leak_sensitive_values: true

secrets:
  cookie:
    - mojaly-kratos-cookie-secret-change-me
  cipher:
    - mojaly-kratos-cipher-secret-0001

ciphers:
  algorithm: xchacha20-poly1305

hashers:
  algorithm: bcrypt
  bcrypt:
    cost: 8

identity:
  schemas:
    - id: default
      url: file:///etc/config/kratos/identity.schema.json

courier:
  smtp:
    connection_uri: smtps://test:test@mailslurper:1025/?skip_ssl_verify=true

session:
  lifespan: 1h
  cookie:
    persistent: false
    same_site: Strict
    path: /